
# Core Framework & Web Server
fastapi>=0.111
uvicorn[standard]>=0.29

# Database & ORM
sqlalchemy[asyncio]>=2.0
alembic>=1.13
asyncpg>=0.29
pgvector>=0.3
starlette==0.52.1

# Data Validation & Configuration
pydantic>=2.0
pydantic-settings>=2.0

# Caching & Message Broker
redis[hiredis]>=5.0
celery[redis]>=5.3

# HTTP Client
httpx>=0.27

# Authentication & Security
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
pyotp>=2.9

# Structured Logging
structlog>=24

# Cloud & Infrastructure
boto3>=1.34

# Email & Notifications
sendgrid>=6.11
firebase-admin>=6.5

# AI & LLM
langchain>=0.2
langchain-anthropic>=0.1
anthropic>=0.28

# Data Processing & Analytics
pandas>=2.2

# PDF & Document Generation
weasyprint>=61
jinja2>=3.1

# Async utilities (for Redis Streams, WebSocket, concurrent tasks)
# These are typically included but explicitly listed for clarity
# aioredis included via redis[hiredis]